{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "prisma-generate": "npx prisma generate --schema ./src/prisma/schema.prisma",
    "prisma-migrate": "",
    "test": "npx prisma generate --schema ./src/prisma/schema.prisma && jest",
    "dev-run": "npm run build && node dist/entrypoint.js",
    "dev-test": "npm run build && npm run test"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "bcrypt": "^5.1.1",
    "express": "^4.18.3",
    "prisma": "^5.10.2"
  }
}
